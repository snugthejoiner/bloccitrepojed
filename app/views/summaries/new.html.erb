<h1>New Summary</h1>

 <div class="row">
   <div class="col-md-4">
     <p>Create a short explanation of your post</p>
   </div>
   <div class="col-md-8">
     <%= form_for [@topic, @post, @summary] do |f| %>
       <div class="form-group">
         <%= f.label :name %>
         <%= f.text_field :name, class: 'form-control', placeholder: "Enter summary name" %>
       </div>
       <div class="form-group">
         <%= f.label :description %>
         <%= f.text_area :description, rows: 8, class: 'form-control', placeholder: "Enter summary description" %>
       </div>
       <%= f.submit "Save", class: 'btn btn-success' %>
     <% end %>
   </div>
 </div>